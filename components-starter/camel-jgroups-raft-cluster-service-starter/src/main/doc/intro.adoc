Spring Boot auto-configuration for the Camel JGroups Raft Cluster Service.

This starter provides leader election for Camel routes using http://www.jgroups.org/raft[JGroups Raft] consensus protocol. It integrates with Camel's `CamelClusterService` SPI so you can use the clustered route controller to ensure only one instance of a route runs across a cluster.
