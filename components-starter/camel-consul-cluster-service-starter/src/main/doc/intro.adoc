Spring Boot auto-configuration for the Camel Consul Cluster Service.

This starter provides leader election and cluster coordination for Camel routes using https://www.consul.io[HashiCorp Consul] as the distributed backend. It integrates with Camel's `CamelClusterService` SPI so you can use the clustered route controller to ensure only one instance of a route runs across a cluster.
